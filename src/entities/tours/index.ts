import { useTours } from './lib/useTours';
import {
  filtersSlice,
  selectFilters,
  setCategories,
  setQuery,
  setOrder
} from './model/filtersSlice';

export {
  filtersSlice,
  selectFilters,
  setCategories,
  setQuery,
  setOrder,
  useTours
};
